# Client Summary Plan

**Goal:** Replace the empty Clients tab with verified aggregate counts while keeping identifier-bearing station details out until authenticated fixtures are available.

- [x] Add failing LuCI tests for total and per-band counts, unknown-value handling and identifier exclusion.
- [x] Render counts from the existing normalized Wi-Fi snapshot.
- [x] Document the authenticated-detail boundary, run all checks, commit and push.
