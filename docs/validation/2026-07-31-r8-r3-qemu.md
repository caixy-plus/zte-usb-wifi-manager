# r8/r3 校准诊断与构建验证（2026-07-31）

## 目的

backend r7 已为 SIM 与 USB 供电 `probe` 补充固定、非敏感、可机器读取的失败码。
主路由器的后续只读证据又证明两个兼容性问题：

1. 匿名 U25S 状态响应会留下关键蜂窝字段为空，SIM 写入校准不能复用只读层的
   “已有任意字段即可”判定，必须先完成真实登录。
2. 官方 OpenWrt 的 `cudy,tr3000-v1-ubootmod` 没有
   `/sys/class/gpio/modem_power/value`；USB VBUS 由 xHCI 消费
   `regulator-fixed` 的 `usb-vbus`。

backend r8 强制 SIM 校准先登录，并增加官方 OpenWrt 的 xHCI bind/unbind
硬件 profile。该 profile 同时检查控制器绑定状态和 `usb-vbus` regulator 状态，
不会把普通总线重置误判为真实断电。

## 诊断契约

- SIM probe 在读取 readiness 字段前强制登录，并区分凭据不可用、认证失败、
  设备读取失败、响应无效、字段缺失、卡槽无效、modem 未连接、网络未注册和
  PPP 未就绪。
- USB power probe 区分板型文件、板型支持、固定控制节点、供电读回、恢复服务和
  设备可达性错误。
- 失败仍返回非零退出码，JSON 只包含固定 `code`，不含密码、Cookie、运营商名称或
  设备标识。

## 本地检查与 GitHub 构建

- 提交：`b19c8a1c3a05b186ffffdf6ee3f1c648e6459afc`
- GitHub Actions：
  [30614580622](https://github.com/caixy-plus/zte-usb-wifi-manager/actions/runs/30614580622)
- `check`：PASS
- OpenWrt 25.12.5 APK SDK：PASS
- OpenWrt 24.10.7 IPK SDK：PASS
- 制品汇总：PASS

下载的 `openwrt-packages` 制品中，`SHA256SUMS` 对两个 APK、两个 IPK 和
`build-manifest.json` 的校验全部通过；构建清单中的源码提交与上述提交完全一致。
本地 `make check` 同样通过。

## 官方 OpenWrt QEMU 验证

两套测试都使用新的 overlay、官方镜像和 GitHub 下载制品。虚拟机先加入禁止访问
私有网段的路由，再更新软件包索引；没有连接主路由器或真实 U25S。

| OpenWrt | 制品 | 安装 | procd/rpcd/ubus | 卸载 | 结果 |
|---|---|---|---|---|---|
| 25.12.5 | r8 APK + r3 APK | PASS | PASS | PASS | PASS |
| 24.10.7 | r8 IPK + r3 IPK | PASS | PASS | PASS | PASS |

验证内容包括：

- 官方镜像 SHA-256 与 `sha256sums` 一致；
- 不使用 `--force-depends` 或 `--force-architecture`；
- 安装后脚本权限、UCI、LuCI 菜单/视图和 rpcd ACL 文件正确；
- `zte-usb-wifi-manager` 由 procd 启动并保持 `running`；
- `zte_usb_wifi` ubus 对象存在，`status` 和 `capabilities` 返回可解析 JSON；
- 默认 `write_enabled=0`、电池策略关闭；
- 正常卸载后软件包和 rpcd/LuCI 入口消失。

## OpenWrt 25.12.5 实机正式升级

Cudy TR3000 v1 `cudy,tr3000-v1-ubootmod` 已用同一份 GitHub APK 正式升级：

- `zte-usb-wifi-manager`：`0.1.0_rc1-r8`（30.33 KiB）
- `luci-app-zte-usb-wifi-manager`：`0.1.0_rc1-r3`（5.33 KiB）

LuCI 上传对话框显示的 APK SHA-256 与本地和 GitHub 汇总值一致。OpenWrt 会拒绝
没有仓库签名的本地 APK，因此只对该已校验文件使用一次性
`apk add --allow-untrusted`；没有修改 apk 的全局信任配置。升级完成后：

- LuCI 页面持续显示 U25S 在线、后端正常、NR5G-SA 和 `eth2` 已连接；
- 凭据文件为 root 所有、权限 `0600`；
- `write_enabled=0`；
- Power Adapter 后端仍为 `unconfigured`；
- `control_path=auto`；
- `calibrated=0`；
- 电池策略仍关闭。

所有一次性任务执行后均删除自身。原有三条 OpenClash 计划任务逐字恢复，五个临时
HTTP 诊断文件均回读为 404。

## 实机只读 probe

USB 供电 probe 返回：

```json
{"ok":true,"mode":"probe","board":"cudy,tr3000-v1-ubootmod","power":1,"recovery_service":true,"netdev_present":true,"device_reachable":true}
```

这证明 r8 能识别官方 ubootmod 板型、当前供电为 ON、恢复服务存在、`eth2` 存在且
U25S 可达；probe 没有执行 OFF/ON。

SIM probe 返回固定失败码 `authentication_failed`。短暂停止监控守护进程后结果
不变，重新安全保存用户提供的凭据后结果也不变，因此已排除守护进程会话竞争和
路由器残留旧凭据。目标固件的 `config.js` 显示 `HAS_LOGIN:!0`，真实 `LD`
响应为空；为避免登录锁定，没有继续猜测密码、变换编码或执行 SIM 写入。

## 尚未完成

真实 SIM 切换与回切、USB OFF/ON、`eth2` 消失/恢复和恢复服务协调仍只能在备用
TR3000 + 备用 U25S 台架完成。完成这些破坏性校准后，还需运行 72 小时真实稳定性
验收，才能发布稳定版。
